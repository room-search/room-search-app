targets:
  $default:
    builders:
      freezed:
        options:
          copy_with: true
          equal: true
      json_serializable:
        options:
          explicit_to_json: true
          field_rename: none
          include_if_null: false
